## 1. Implementation

- [ ] 1.1 Add error `DisallowedTransferFromSource(address target, address from)` to `src/interfaces/ISyndicateVault.sol`, alongside the existing guard errors (`DisallowedTransferTarget`, `DisallowedBatchTarget`, `MalformedCall`).
- [ ] 1.2 In `src/SyndicateVault.sol::_guardBatchCalls`, extend the **Part-1 unconditional loop** (the privileged-target pass, ABOVE the `tierRegistry` staticcall and both degrade-open returns): for each call with `data.length >= 4`, if `bytes4(data[0:4]) == _SEL_TRANSFER_FROM`, then (a) `if (data.length < 68) revert MalformedCall();` and (b) decode `from = address(uint160(uint256(bytes32(data[4:36]))))` and `if (from != address(this)) revert DisallowedTransferFromSource(calls[i].target, from);`. Do NOT touch Part 2's `transferFrom` branch (destination guard stays as is).
- [ ] 1.3 Update the `_guardBatchCalls` natspec per house style ("every guard states its adversary"): document the LP-allowance-confiscation adversary (delegatecall ⇒ `msg.sender == vault` ⇒ `transferFrom(victim, vault, bal)` spends `allowance[victim][vault]`; net-outflow reads 0 because the balance rises; tier coverage prices vault capital, never third-party wallets), state that the source check is unconditional for the same reason Part 1 is (refusing a capability, not pricing a call), and state why the permitted source is exactly the vault and NOT `isAdapterAllowed` (destination consent ≠ source consent; no honest batch pulls from a non-vault source — adapters pull in their own code and return by push). Also update the Part-2 comment's transferFrom line to note the source is guarded in Part 1.

## 2. Tests — `test/vault/SelectorGuard.t.sol`

- [ ] 2.1 **Deliberate removal of an asserted-safe path**: replace `test_transferFromIntoVaultPasses` (lines 144-153) with `test_transferFromThirdPartySourceReverts_LPAllowanceConfiscation`: same fixture shape (third-party `sink` funded and granting the vault an allowance — use `type(uint256).max` to mirror the real deposit-allowance pattern), batch runs `usdc.transferFrom(sink, vault, 100e6)`, expect revert `DisallowedTransferFromSource(address(usdc), sink)`, and assert `sink`'s balance and allowance are untouched. The natspec MUST record that the predecessor test pinned issue #115's hole as intended behaviour ("pull into the vault always passes") and that the behaviour was removed deliberately because it is the confiscation primitive.
- [ ] 2.2 New: `transferFrom` from an ALLOWLISTED ADAPTER source reverts — `usdc.transferFrom(adapter, vault, 1e6)` reverts `DisallowedTransferFromSource(address(usdc), adapter)`. Pins Decision 1: destination allowlisting confers no source consent (guards against a future "symmetric" refactor).
- [ ] 2.3 New: vault-sourced `transferFrom` passes end-to-end — batch of `[usdc.approve(vault, 1e6), usdc.transferFrom(vault, adapter, 1e6)]` succeeds (the self-approve creates `allowance[vault][vault]`; recipient `adapter` is allowlisted); assert the adapter received 1e6. Pins Decision 4 (the `from == vault` exemption).
- [ ] 2.4 New: vault-sourced `transferFrom` to a non-allowlisted recipient still reverts `DisallowedTransferTarget(usdc, SEL_TRANSFER_FROM, attacker)` — the destination guard is unchanged.
- [ ] 2.5 New: registry-less coverage, one test per degrade-open branch (mirroring the #93 pattern): (a) `tierRegistry` set to `address(0)` and (b) governor without the `tierRegistry()` getter — in both, `usdc.transferFrom(sink, vault, amt)` still reverts `DisallowedTransferFromSource`. Pins Decision 2 (unconditional placement; the audit-diff placement would fail these).
- [ ] 2.6 Update `test_transferFromWithOnlyOneArgReverts` if needed (it should still revert `MalformedCall`, now from Part 1) and add its registry-less twin: short `transferFrom` calldata with the registry unset reverts `MalformedCall` (previously executed). Pins the accepted side effect in Decision 2.

## 3. Verification

- [ ] 3.1 `forge build` and full `forge test` pass (CI-matching forge; serialize `via_ir` builds — guard with `while pgrep -x forge >/dev/null; do sleep 30; done`, run in foreground, never judge a build through a piped exit code).
- [ ] 3.2 Confirm no other test or fixture batches a raw `transferFrom` (`grep -rn "transferFrom" test/` — as of scaffolding, only `test/vault/SelectorGuard.t.sol` does; `test/audit-fixes/Vault_batchQueueTargets.t.sol` grants the max allowance but never batches the pull, so it must keep passing untouched).
- [ ] 3.3 `openspec validate guard-transferfrom-source --strict` passes.
- [ ] 3.4 `forge fmt --check` clean on `src/` and `test/` (fmt with a CI-matching forge).
