## 1. Implementation

- [x] 1.1 Add error `DisallowedTransferFromSource(address target, address from)` to `src/interfaces/ISyndicateVault.sol`, alongside the existing guard errors (`DisallowedTransferTarget`, `DisallowedBatchTarget`, `MalformedCall`).
- [x] 1.2 In `src/SyndicateVault.sol::_guardBatchCalls`, extend the **Part-1 unconditional loop** (the privileged-target pass, ABOVE the `tierRegistry` staticcall and both degrade-open returns): for each call with `data.length >= 4`, if `bytes4(data[0:4]) == _SEL_TRANSFER_FROM`, then (a) `if (data.length < 68) revert MalformedCall();` and (b) decode `from = address(uint160(uint256(bytes32(data[4:36]))))` and `if (from != address(this)) revert DisallowedTransferFromSource(calls[i].target, from);`. Do NOT touch Part 2's `transferFrom` branch (destination guard stays as is).
- [x] 1.3 Update the `_guardBatchCalls` natspec per house style ("every guard states its adversary"): document the LP-allowance-confiscation adversary (delegatecall ⇒ `msg.sender == vault` ⇒ `transferFrom(victim, vault, bal)` spends `allowance[victim][vault]`; net-outflow reads 0 because the balance rises; tier coverage prices vault capital, never third-party wallets), state that the source check is unconditional for the same reason Part 1 is (refusing a capability, not pricing a call), and state why the permitted source is exactly the vault and NOT `isAdapterAllowed` (destination consent ≠ source consent; no honest batch pulls from a non-vault source — adapters pull in their own code and return by push). Also update the Part-2 comment's transferFrom line to note the source is guarded in Part 1.

## 2. Tests — `test/vault/SelectorGuard.t.sol`

- [x] 2.1 **Deliberate removal of an asserted-safe path**: replace `test_transferFromIntoVaultPasses` (lines 144-153) with `test_transferFromThirdPartySourceReverts_LPAllowanceConfiscation`: same fixture shape (third-party `sink` funded and granting the vault an allowance — use `type(uint256).max` to mirror the real deposit-allowance pattern), batch runs `usdc.transferFrom(sink, vault, 100e6)`, expect revert `DisallowedTransferFromSource(address(usdc), sink)`, and assert `sink`'s balance and allowance are untouched. The natspec MUST record that the predecessor test pinned issue #115's hole as intended behaviour ("pull into the vault always passes") and that the behaviour was removed deliberately because it is the confiscation primitive.
- [x] 2.2 New: `transferFrom` from an ALLOWLISTED ADAPTER source reverts — `usdc.transferFrom(adapter, vault, 1e6)` reverts `DisallowedTransferFromSource(address(usdc), adapter)`. Pins Decision 1: destination allowlisting confers no source consent (guards against a future "symmetric" refactor).
- [x] 2.3 New: vault-sourced `transferFrom` passes end-to-end — batch of `[usdc.approve(vault, 1e6), usdc.transferFrom(vault, adapter, 1e6)]` succeeds (the self-approve creates `allowance[vault][vault]`; recipient `adapter` is allowlisted); assert the adapter received 1e6. Pins Decision 4 (the `from == vault` exemption).
- [x] 2.4 New: vault-sourced `transferFrom` to a non-allowlisted recipient still reverts `DisallowedTransferTarget(usdc, SEL_TRANSFER_FROM, attacker)` — the destination guard is unchanged.
- [x] 2.5 New: registry-less coverage, one test per degrade-open branch (mirroring the #93 pattern): (a) `tierRegistry` set to `address(0)` and (b) governor without the `tierRegistry()` getter — in both, `usdc.transferFrom(sink, vault, amt)` still reverts `DisallowedTransferFromSource`. Pins Decision 2 (unconditional placement; the audit-diff placement would fail these).
- [x] 2.6 Update `test_transferFromWithOnlyOneArgReverts` if needed (it should still revert `MalformedCall`, now from Part 1) and add its registry-less twin: short `transferFrom` calldata with the registry unset reverts `MalformedCall` (previously executed). Pins the accepted side effect in Decision 2.

## 3. Verification

- [x] 3.1 `forge build` and full `forge test` pass (CI-matching forge; serialize `via_ir` builds — guard with `while pgrep -x forge >/dev/null; do sleep 30; done`, run in foreground, never judge a build through a piped exit code). Result: `forge build` clean; full `forge test` — 122 suites, 1675 passed, 9 failed, 5 skipped (1689 total). The 9 failures are all in `test/integration/strategies/UniswapAdapterFork.t.sol`, a live-fork suite requiring `--fork-url $BASE_RPC_URL` (see `.github/workflows/fork-tests.yml`) that this repo's own main CI (`ci.yml`) excludes via `--no-match-path "test/integration/**"` — pre-existing, unrelated to this change (no RPC configured in this environment). All 19 `test/vault/SelectorGuard.t.sol` tests pass.
- [x] 3.2 Confirm no other test or fixture batches a raw `transferFrom` (`grep -rn "transferFrom" test/` — as of scaffolding, only `test/vault/SelectorGuard.t.sol` does; `test/audit-fixes/Vault_batchQueueTargets.t.sol` grants the max allowance but never batches the pull, so it must keep passing untouched). Confirmed: grep shows no other test file routes a raw `transferFrom` through `executeGovernorBatch`.
- [x] 3.3 `openspec validate guard-transferfrom-source --strict` passes.
- [x] 3.4 `forge fmt --check` clean on `src/` and `test/` (fmt with a CI-matching forge).

## 4. Post-merge remediation (Pashov audit of PR #157, issue #115)

- [x] 4.1 **Finding 1 [90, 3-agent convergence]**: extend `_guardBatchCalls`'s guarded-selector set (Option A) to also recognize Permit2 `AllowanceTransfer.transferFrom` (`0x36c78516`), Permit2 `AllowanceTransfer.approve` (`0x87517c45`), DSToken `pull` (`0xf2d5d56b`), and DSToken `move` (`0xbb35783b`) — verified via `cast sig`. Part 1b's source decode covers `transferFrom`/Permit2-`transferFrom`/DSToken-`pull`/DSToken-`move` (all place source at bytes 4:36). Part 2's destination decode covers `transferFrom`/Permit2-`transferFrom`/DSToken-`move` (bytes 36:68) plus a dedicated Permit2-`approve` branch (spender at bytes 36:68, since Permit2's leading `token` arg shifts it one slot right of legacy `approve`). Documented Option B (target-based gating, recommended by the audit as more durable) as a required follow-up, deferred because it would re-plumb tier-pricing/TierRegistry semantics outside this change's footprint — see design.md decision 6.
- [x] 4.2 **Finding 2 [80]**: scope the Part 2 self-transfer fast-path to `asset()` only — `if (recipient == address(this) && calls[i].target == asset()) continue;` — so a non-`asset()` token decoding a vault-to-vault `transferFrom` no longer skips the TierRegistry check. See design.md decision 7.
- [x] 4.3 Update `_guardBatchCalls` natspec (Part 1b, Part 2, RESIDUAL) to document the new selectors, the Option A/B decision and rationale, and the asset()-scoped fast-path.
- [x] 4.4 Add adversarial tests to `test/vault/SelectorGuard.t.sol`: a Permit2-routed `transferFrom` from a non-vault source reverts `DisallowedTransferFromSource`; a Permit2 `approve` to a non-allowlisted spender reverts `DisallowedTransferTarget`; a DSToken `pull`/`move` from a non-vault source reverts `DisallowedTransferFromSource`; a non-`asset()` token's `transferFrom(vault, vault, amount)` now reverts `DisallowedTransferTarget` instead of passing the old unconditional fast-path; `asset()`'s own self-transfer fast-path still passes. No existing passing test weakened or removed.
- [x] 4.5 Update `specs/syndicate-vault/spec.md`, `design.md`, and `proposal.md` to reflect both fixes (new scenarios, decision record, impact note).
- [x] 4.6 `forge build`, full `forge test --no-match-path "test/integration/**"`, `forge fmt`, `./script/check-layout-goldens.sh`, and `openspec validate --changes --strict` all clean.
